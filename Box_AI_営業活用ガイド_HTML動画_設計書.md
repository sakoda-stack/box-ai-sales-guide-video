# Box AI 営業活用ガイド HTML動画 設計書

| 項目 | 内容 |
|---|---|
| 文書版 | 1.5 |
| 作成日 | 2026-08-14 |
| 更新日 | 2026-08-15 |
| 対象 | 営業部門向け社内共有 |
| 状態 | 実装準拠版 |
| 対象HTML | `Box_AI_営業活用ガイド_動画.html` |

## 1. 目的

Box AIの基本的な仕組み、営業での実務活用、具体的なプロンプト、利用制限、情報整理の考え方を、実画面・図解・ナレーション音声・同期字幕でわかりやすく説明する。

視聴後に、営業担当者が次の内容を判断できる状態を目指す。

- Box AIで何ができるか、どのような仕組みで動くか
- 複数資料をどう集約できるか
- PJコードをなぜ・どこに記載すべきか
- どこからBox AIを開き、どの程度具体的に指示するか
- 性能制限(Box Agent Lite)表示時にどう対応するか
- 今後、どの情報をBoxへ蓄積・整理するか

## 2. 対象者と前提

| 区分 | 内容 |
|---|---|
| 主対象 | Boxを日常利用する営業担当者 |
| 前提 | 生成AIの一般的な注意事項は別途周知済み |
| 推奨ブラウザ | Microsoft Edge(Chromiumベースのブラウザで動作) |
| 利用方法 | ローカルHTML、社内共有先、またはWeb公開先からブラウザで開く |
| 前提データ | Box上で閲覧権限のあるファイル・フォルダ |
| 非対象 | Box AIの契約管理、権限設定、モデル内部構成、生成結果の最終承認方法 |

## 3. 成果物

### 3.1 HTML動画本体

- ファイル名:`Box_AI_営業活用ガイド_動画.html`
- 全12シーン、総再生時間 約6分(推定尺 約369秒。音声再生成後は実測値に置き換わる)
- 16:9レイアウト、1280×720を基準にレスポンシブ表示
- キネティック・タイポグラフィ演出:ナレーションの文単位キューに同期して、大きな文字・要素をJS駆動で順次表示/切替(`data-at`/`data-out`属性。速度変更・シークにも追従)
- 事前生成したMP3ナレーション(edge-tts / ja-JP-KeitaNeural)を`<audio>`で再生
- 再生速度を1倍 / 1.25倍 / 1.5倍で切替可能(キー:S)
- 文単位で同期する字幕(CC)を表示。ON/OFF切替可能。字幕は画面下部の専用帯に表示し、シーン本文と重ならない
- 実際のBox画面、成果物画面、Box Agent Lite警告画面を使用
- 実例HTML(目白御留山PJ 総合概要資料)を画面内から保存可能(大型ボタン)
- 関連リンクを画面内に表示:「Box Agentとは」(box.com公式)、「Box Agent ユースケース資料」(社内Box)
- 操作パネルはマウス操作時のみ右下に表示し、約1.8秒で自動的に消える。タップ環境では画面タップで表示/非表示を切り替え、約3.2秒で自動的に消える
- 画面上部にパンくず(チャプター)を常時表示。現在地をハイライトし、クリック/タップで任意のシーンへ移動できる
- `captions.js`が音声なしで生成されている場合は、ブラウザの音声合成(Web Speech API)で読み上げる。日本語音声が無い環境では字幕+自動進行の無音モードになる(開始ゲートの表示も切り替わる)
- Box公式ビジュアル2点(`box-ai-hub.png` / `box-ai-agent-layers.png`)をScene02・03で使用

### 3.2 公開・配布用ファイル

| ファイル | 用途 |
|---|---|
| `index.html` | 公開URL直下から動画HTMLへリダイレクト(GitHub Pages想定) |
| `.nojekyll` | GitHub PagesでのJekyll処理を無効化 |
| `Box_AI_営業活用ガイド_HTML動画.zip` | HTML本体と`assets/`一式を収録した配布用ZIP |

### 3.3 ファイル構成

```text
Box_AI_営業活用ガイド_動画.html
index.html                     … 動画HTMLへのリダイレクト
.nojekyll
tools/
├─ script_segments.py          … 原稿(シーン単位)の正本
├─ gen_audio.py                … ナレーションMP3・字幕データ生成(要インターネット)
├─ gen_audio.bat               … Windows向け:上記をダブルクリックで実行
└─ estimate_captions.py        … 音声なしで推定タイミングの字幕のみ生成
assets/
├─ box-ai-hero.png
├─ box-ai-home.png
├─ box-ai-hub.png              … Box公式:あらゆる形式の資料 × 複数のAIモデル
├─ box-ai-agent-layers.png     … Box公式:エージェントの構成レイヤー図
├─ box-folder-ai-button.png
├─ box-folder-ai-dialog.png
├─ mejiro-project-output.png
├─ pj-code-hub.png
├─ box-agent-lite-warning.png
├─ mejiro-otomeyama-pj-overview.html
└─ audio/
   ├─ captions.js              … 各シーンの尺と文単位の字幕キュー
   └─ scene-01.mp3 〜 scene-12.mp3
```

> HTML本体と`assets`フォルダの相対位置は変更しない。フォルダだけを別の場所へ移動すると、画像・音声・字幕・実例HTMLのダウンロードが動作しなくなる。

## 4. 全体構成

```mermaid
flowchart LR
    A[検索だけじゃない、BOX AI Agent] --> B[Box AIの仕組み]
    B --> B2[エージェントの構成]
    B2 --> C[点在する資料の横断整理]
    C --> D[PJコードが共通キー]
    D --> E[具体的な使い方]
    E --> F[実際にやってみた例]
    F --> G[Boxへ集める]
    G --> H[プロンプトのコツ]
    H --> I[毎回の説明が必要]
    I --> J[1日の利用制限]
    J --> K[まずは身近なところから]
```

## 5. 機能要件

| ID | 機能 | 要件 |
|---|---|---|
| FR-01 | シーン再生 | 12シーンを順番に表示し、最終シーンで停止する |
| FR-02 | 音声同期 | 各シーンのMP3再生終了(`ended`)を待って次シーンへ進む |
| FR-03 | 再生操作 | 再生・一時停止・前へ・次へを提供する |
| FR-04 | 進捗表示 | 全体プログレスバー(クリックでシーン移動)、経過時間、総時間、シーン番号・タイトルを表示する |
| FR-05 | 音声切替 | 音声のON/OFF(ミュート)を再生中に切り替えられる |
| FR-06 | 字幕 | ナレーションと文単位で同期した字幕を表示し、ON/OFFを切り替えられる |
| FR-07 | 全画面 | ブラウザのFullscreen APIで全画面表示できる |
| FR-08 | 自動開始 | 開始ゲートは設けず、読み込み後すぐに再生を始める（ブラウザの自動再生制限に合わせ、音声はミュート状態で開始）。音声は操作パネルのスピーカーボタン（またはMキー）でONにする |
| FR-09 | 実例保存 | 目白御留山PJの実例HTMLを画面内から保存できる |
| FR-10 | フォールバック | 音声ファイルの読込に失敗した場合、`captions.js`の尺(未読込時は既定値)によるタイマー進行へ切り替える |
| FR-11 | キーボード操作 | Space、左右キー、M、C、F、Sに対応する |
| FR-12 | 再生速度 | 1倍・1.25倍・1.5倍を循環切替できる(音声・字幕・演出の同期を維持) |
| FR-13 | キネティック演出 | `data-at`/`data-out`属性の秒指定でナレーションに同期して要素を表示/切替する |
| FR-14 | 関連リンク | Box公式「Box Agentとは」とユースケース資料(社内Box)へ新規タブで遷移できる |
| FR-15 | パンくず | 画面上部にチャプターを並べ、現在地を強調表示し、クリックで該当シーンへ移動できる |
| FR-16 | 尺のスケーリング | 各シーンの`data-plan`(想定尺)と実際のナレーション尺の差を係数で吸収し、音声を再生成しても演出タイミングが崩れないようにする |
| FR-17 | 音声状態フラグ | `captions.js`の`window.NARRATION_AUDIO`が`false`のとき、MP3を読み込まない |
| FR-18 | ブラウザ読み上げ | MP3が無い場合、Web Speech APIの日本語音声（Edgeの「Microsoft Keita Online (Natural)」= edge-ttsの`ja-JP-KeitaNeural`と同一の音声を最優先で選択）でナレーションする。`boundary`イベントで字幕・演出を読み上げ位置に同期し、`end`で次シーンへ進む。一時停止・ミュート・シーク・チャプター移動に追従する |

## 6. シーン設計

シーン内の要素は`reveal`クラスと`--delay`(秒)により、ナレーションの進行に合わせて順次表示する。

### Scene 01:検索だけじゃない、BOX AI Agent

| 項目 | 内容 |
|---|---|
| 目的 | 検索だけのAIではなく、確認・要約・抽出・整理までチャット形式で行えることを伝える |
| 表示 | ヒーロー背景(`box-ai-hero.png`)+実際のBox AIトップ画面(`box-ai-home.png`)とAIメニューの強調枠 |
| 要素 | タイトル「検索だけじゃない、BOX AI Agent」、対象資料チップ(物件資料・価格表・PJコード・サービス枠管理表・稟議関連資料)、「確認・要約・抽出・整理」 |

### Scene 02:Box AIの仕組み

| 項目 | 内容 |
|---|---|
| 目的 | 単体で回答するチャットAIとの違い(あらゆる形式の資料を参照し、複数モデルで処理)を伝える |
| 表示 | Box公式ビジュアル`box-ai-hub.png`(Box AIを中心にPDF・Excel・PowerPoint等と複数のAIモデルが接続) |
| 要点 | ①あらゆる形式の資料を参照 ②複数のAIモデル+エージェント機能 ③機密情報の取り扱い・画像の読み込みも複数エージェントで処理し精度が高い |
| リンク | Box公式「Box Agentとは」 |

### Scene 03:エージェントの構成

| 項目 | 内容 |
|---|---|
| 目的 | エージェントが層で構成されること、コンテンツ参照が権限の範囲内であること、指示で結果が変わることを伝える |
| 表示 | Box公式ビジュアル`box-ai-agent-layers.png`(AI MODEL / INSTRUCTIONS / OBJECTIVE / CAPABILITIES & TOOLS / SECURE ACCESS TO YOUR CONTENT) |
| 要点 | ①コンテンツへの安全なアクセス(土台) ②AIモデル ③指示・目的 ④機能とツール |
| 締め | 「資料が整理されているほど、活用しやすくなる」 |

### Scene 04:点在する資料の横断整理

| 項目 | 内容 |
|---|---|
| 目的 | フォルダをまたいで散らばった資料を、AIがまわりながら横断参照してまとめられることを伝える |
| 演出 | 中央のAIノードの周囲に資料を円環配置し、走査光が一周しながら各資料を順に光らせる(オービット表現) |
| 入力例 | 物件資料、価格表、PJコード一覧、サービス枠管理表、広告宣伝費、工程表、収支表、契約書 |
| 出力例 | 物件ごとの基本情報、価格、PJコード、確認事項 |
| 締め | 「一つずつ開いて確認する手間を減らし、情報整理の初動を早める」 |

### Scene 05:PJコードが共通キー

| 項目 | 内容 |
|---|---|
| 目的 | 資料同士を紐づける共通キーとしてPJコードを記載する習慣づけ |
| 表示 | PJコードを中心にした紐づけイメージ(`pj-code-hub.png`)、PJコード表記は`3320×××` |
| 記載対象 | 資料名、フォルダ名、ファイル内の記載、メールのタイトル・本文 |
| 効果 | 検索・抽出・集約の精度向上 |

### Scene 06:具体的な使い方

| 項目 | 内容 |
|---|---|
| 目的 | 対象を選んで具体的に質問する、という基本操作を理解させる |
| 表示 | フォルダの「Box AIに質問」ボタン(`box-folder-ai-button.png`)、質問ダイアログ(`box-folder-ai-dialog.png`) |
| 質問例 | 要約、物件名とPJコードの一覧化、価格情報の整理、サービス枠残額の確認、稟議・決裁資料の下書き作成 |
| 現状の制約 | 今できるのはここまで。出力形式もまだ限定的で、実際のファイルを編集するなどタスクそのものの完了はできない |

### Scene 07:実際にやってみた例

| 項目 | 内容 |
|---|---|
| 目的 | 実際に行った活用例を示し、自分の業務への適用イメージを持たせる |
| 活用例 | ①物件リストにPJコード・価格を付与し顧客情報を紐付け ②物件概要フォルダを指定し中のファイルの概要をHTMLに集約 ③事業収支・収支経緯表・サービス枠管理表を分析し、決裁の定型文に合わせて文章と数字を出力(そのままラクモのワークフローに貼る形へ調整できる) |
| 表示 | ミニブラウザ枠内に目白御留山PJ 総合概要資料(`mejiro-project-output.png`) |
| 保存 | 「⇩ 実例HTMLを保存」ボタンから`assets/mejiro-otomeyama-pj-overview.html`を`目白御留山PJ_総合概要資料.html`として保存(`download`属性) |

### Scene 08:Boxへ集める

| 項目 | 内容 |
|---|---|
| 目的 | AIはBoxの権限の中で動くという前提を示し、自分の権限が及ぶ範囲へ情報を蓄積する行動を促す |
| 集約対象 | 物件・PJの資料、メール本文、添付資料、価格表、稟議関連資料 |
| 図解 | 資料チップ → 下向き矢印 → 「AIは、Boxの権限の中で動く」 |
| 補足① | AIが突合しやすいよう、PJコードなど一定のキーを中心に整理しながら格納する |
| 補足② | GoogleワークスペースとBoxの連携は許可済みで、Gmailの本文をBoxに蓄積する機能もある |

### Scene 09:プロンプトのコツ

| 項目 | 内容 |
|---|---|
| 目的 | 漠然と質問せず、対象・項目・出力の形を具体的に書くことで実務で使いやすい結果を得る |
| 3つのポイント | ①対象資料 ②確認したい項目 ③出力の形 |
| プロンプト例 | 「このフォルダの内容を突合してください。そのうえで、決裁資料用の売価・税額・サービス枠残額・粗利などの数字を、まとめて表示してください。出力は一覧表でお願いします。」 |

### Scene 10:毎回の説明が必要(利用時の注意①)

| 項目 | 内容 |
|---|---|
| 目的 | コンテキストが蓄積されない前提を示し、初回プロンプトを作り込む運用を促す |
| 3つの注意 | ①背景・前提は蓄積されない ②毎回のプロンプトでしっかり説明する ③初回のプロンプトが肝心(事前にChatGPTなどで作成) |
| 締め | 「うまく出力できたプロンプトは、ぜひ共有していきましょう」 |

### Scene 11:1日の利用制限(利用時の注意②)

| 項目 | 内容 |
|---|---|
| 目的 | 1日の利用制限とモデル低下を、実画面を先に見せてから説明する |
| 構成 | 前半:Box Agent Lite警告画面(`box-agent-lite-warning.png`)を画面中央に大きく表示 → 後半:縮小して左に置き、右側で対応方針を説明 |
| 制限中の方針 | 横断整理・複雑な下書き・HTMLまとめなど重い作業は避け、簡単な要約・資料確認など軽めにとどめる |
| 締め | 「今後、プロンプトを含めて改善されていく見込み」 |

### Scene 12:まずは身近なところから

| 項目 | 内容 |
|---|---|
| 目的 | 小さく始めてBoxへ整理・蓄積する行動を促して締める |
| ステップ | ①身近なフォルダ・管理表から試す → ②無理のない範囲で使ってみる → ③Boxへ資料を整理・蓄積 |
| 最終メッセージ | 「活用事例・うまくいったプロンプトは、ぜひご共有ください」 |
| リンク | Box公式「Box Agentとは」、ユースケース資料(社内Box)、実例HTML保存 |

## 7. UI・ビジュアル設計

### 7.1 画面基準

| 項目 | 仕様 |
|---|---|
| 基準サイズ | 1280×720、16:9 |
| レスポンシブ | ウィンドウ幅に応じ、比率を維持して縮小(コンテナクエリ単位`cqw`使用) |
| 背景 | 濃紺から青へのグラデーション(ダークシーン)/白基調(ライトシーン) |
| 書体 | `Yu Gothic UI`を中心とするシステムフォント |
| 字幕 | 画面下部に半透明の濃紺帯+白文字で表示 |
| スクリーンショット | 内容を改変せず、枠・ラベルで説明対象を強調 |

### 7.2 カラーパレット

| 用途 | 色 |
|---|---|
| メイン背景 | Navy `#071A3B` 系 |
| Box・操作強調 | Blue `#1769FF` 系 |
| AI・情報移動 | Cyan `#67E3FF` / `#1FD2FF` 系 |
| 重要ポイント | Orange 系 |
| 資料背景 | 白〜淡青 |
| 警告 | Red系。実画面の警告色を基準にする |

## 8. アニメーション設計

- シーン切替:フェード+短い上方向移動(`scene-enter`)
- 要素表示:`reveal`+`--delay`により、ナレーション進行と合わせて順次表示
- 図解:矢印上のドット移動で情報の流れを表現
- 警告:激しい点滅は使わず、枠と短いパルスで強調
- `prefers-reduced-motion`が有効な環境では装飾アニメーションを抑制

アニメーションは装飾目的ではなく、「どこを操作するか」「どの資料が参照されるか」「情報がどこへ集まるか」「何が成果物か」の理解を補助するために使用する。

## 9. 音声・字幕・再生制御設計

### 9.1 ナレーション音声(事前生成MP3方式)

| 項目 | 仕様 |
|---|---|
| 生成方法 | `tools/gen_audio.py`(Python + `edge_tts`)で事前生成。Windowsは`tools/gen_audio.bat`をダブルクリックでも生成できる |
| 音声 | `ja-JP-KeitaNeural` |
| 出力 | `assets/audio/scene-01.mp3`〜`scene-12.mp3` |
| 字幕データ | 生成時のSentence/WordBoundaryイベントから文単位のキュー(開始・終了時刻・テキスト)を抽出し、各シーンの尺とあわせて`assets/audio/captions.js`(`window.NARRATION`)に出力 |
| 同期 | `<audio>`の`ended`イベントで次シーンへ進む。字幕は`timeupdate`相当のフレーム更新で現在時刻に一致するキューを表示 |
| エラー時 | `error`イベントでタイマー進行(無音)へ切り替える |
| 総尺 | 約369秒(約6分09秒。推定値。音声再生成後は実測値に更新される) |
| 原稿の正本 | `tools/script_segments.py`の`SEGMENTS`(12シーン) |
| 音声なし運用 | `tools/estimate_captions.py`で推定タイミングの`captions.js`のみ生成できる。この場合`window.NARRATION_AUDIO = false`が出力され、再生はブラウザ読み上げ(日本語音声が無ければ字幕+タイマー進行)になる |

> 旧v1.0のブラウザ音声合成(`speechSynthesis`)方式は廃止した。環境依存で音質・音声選択が不安定なため、事前生成MP3方式に変更している。原稿(`tools/script_segments.py`)を変更した場合は`python tools/gen_audio.py`を再実行し、MP3と`captions.js`を再生成する。ネットワークが使えない環境では`python tools/estimate_captions.py`で字幕のみ更新し、後日あらためて音声を生成する。

### 9.2 開始方法

開始ゲートは設けず、読み込み後すぐに再生する。音声付きの自動再生はブラウザに止められるため、音声はミュート状態で開始し、利用者がスピーカーボタン(またはMキー)を押した時点で有効化する。押した操作がユーザー操作として扱われるため、MP3再生・ブラウザ読み上げのどちらも確実に開始できる。

- 起動直後に、音声をONにできる旨のガイドを数秒表示し、操作パネルも一時的に表示する
- 音声をONにすると、使用中の音声名(例:Microsoft Keita Online (Natural))を通知する

### 9.3 再生状態

| 状態 | 動作 |
|---|---|
| 開始直後 | ミュート状態で自動再生し、音声ONのガイドを表示 |
| 再生中 | 経過時間、進捗バー、シーン番号・タイトル、字幕を更新 |
| 音声再生 | MP3の再生完了を待って次シーンへ進む |
| 音声読込失敗 | `captions.js`の尺によるタイマーで次シーンへ進む |
| 一時停止 | 進行と音声を停止し、同じ位置から再開 |
| 前後移動・シーク | 現在の音声を停止し、移動先シーンの先頭(またはシーク位置)から再開 |
| 終了 | Scene 12を表示した状態で停止 |

## 10. 操作仕様

| 操作 | 画面UI | キー |
|---|---|---|
| 再生・一時停止 | 再生ボタン | Space |
| 前のシーン | ◀ボタン | ← |
| 次のシーン | ▶ボタン | → |
| 音声ON/OFF | スピーカーボタン（音声OFFの間は点滅して位置を知らせる） | M |
| 字幕ON/OFF | 「字」ボタン | C |
| 再生速度切替 | 「1×」ボタン(1→1.25→1.5倍) | S |
| 全画面 | ⛶ボタン | F |
| シーン移動 | プログレスバーをクリック | ― |
| 実例HTML保存 | Scene 07・12の「⇩ 実例HTMLを保存」 | クリック |
| チャプター移動 | 画面上部のパンくずをクリック | ― |
| 操作パネル表示 | マウス移動(自動非表示)/タップでトグル | ― |

## 11. アクセシビリティ

- 操作ボタンには`aria-label`を設定する
- 字幕領域は`aria-live="polite"`とする
- 主要画像には内容を表す代替テキストを設定する
- 色だけに依存せず、ラベル、枠、本文でも状態を示す
- 文字と背景のコントラストを確保する
- キーボードでも主要操作を実行できるようにする
- `prefers-reduced-motion`を尊重する

## 12. 配布・起動方法

### Web公開(GitHub Pages想定)

1. リポジトリ一式(HTML本体、`index.html`、`.nojekyll`、`assets/`)をそのまま公開する
2. 公開URLを開くと`index.html`が動画HTMLへリダイレクトする

### 単体利用(ZIP)

1. `Box_AI_営業活用ガイド_HTML動画.zip`を展開する
2. `Box_AI_営業活用ガイド_動画.html`をブラウザ(推奨:Microsoft Edge)で開く
3. 「音声付きで再生」または「音声なしで再生」を選ぶ

### 更新時の注意

- HTML本体と`assets/`フォルダは同一階層のまま共有する
- 原稿を変更した場合は`tools/gen_audio.py`でMP3・字幕を再生成する
- 画像や実例HTMLを差し替える場合は、ファイル名またはHTML内の参照先を合わせる

## 13. テスト・受入基準

| ID | 分類 | 受入基準 |
|---|---|---|
| QA-01 | 表示 | 1280×720で12シーンすべてが枠内に収まり、文字・画像・操作部・パンくずが重ならない |
| QA-02 | 画像 | ヒーロー、Boxトップ、フォルダ操作、実例、PJコード図、Lite警告の画像が欠けずに表示される |
| QA-03 | 進行 | 各シーンのMP3再生完了後に次シーンへ進む。音声読込失敗時はタイマーで進行する |
| QA-04 | 操作 | 再生、停止、前後移動、音声切替、字幕切替、全画面、プログレスバー移動、キーボード操作が動作する |
| QA-05 | 音声 | 全12シーンのナレーションが省略なく再生される(MP3未生成時はブラウザ読み上げ、日本語音声が無い環境では字幕+タイマー進行) |
| QA-06 | 字幕 | ナレーションと同期して文単位の字幕が表示され、CCボタンで切り替えられる |
| QA-07 | 保存 | Scene 07・12のボタンから実例HTMLを保存できる |
| QA-08 | 配布 | ZIPに本体HTMLと`assets/`一式が含まれ、展開後も動作する |
| QA-09 | 安定性 | ブラウザの開発者コンソールに未処理エラーが出ない |
| QA-10 | 軽減設定 | `prefers-reduced-motion`有効時に不要な動きが抑えられる |

## 14. 既知の制約

- ナレーションはMicrosoftのオンラインTTS(edge-tts)で事前生成したもの。再生成にはネットワーク接続と`edge_tts`パッケージが必要
- ブラウザの自動再生制限により、初回は利用者のクリックが必要
- HTML動画はブラウザ再生用であり、MP4を直接生成するものではない
- BoxのUIや利用枠表示が変更された場合、スクリーンショットと説明文の更新が必要
- Box Agent Lite時の実際の性能は環境や処理内容により異なるため、動画内では重い作業を避ける運用基準として説明する

## 15. 読み上げ原稿

原稿の正本は`tools/script_segments.py`の`SEGMENTS`とする。以下はその写しである。

### Scene 01:検索だけじゃない、BOX AI Agent

BOX AI Agentは、検索だけのAIではありません。Box上に保存されている物件資料や価格表、PJコード、サービス枠管理表、稟議関連資料などをもとに、必要な情報の確認や要約、抽出、複数資料の整理まで、チャット形式で行うことができます。

### Scene 02:Box AIの仕組み

Box AIは、単体で回答する一般的なチャットAIとは違い、Box上のPDFやExcel、パワーポイントなど、あらゆる形式の資料を参照しながら処理します。しかも、その裏側では複数のAIモデルやエージェント機能を組み合わせています。機密情報の取り扱いや画像の読み込みにも複数のエージェントを活用しているため、精度の高い結果が得られやすい仕組みです。

### Scene 03:エージェントの構成

Box AI Agentは、いくつかの層が重なってできています。土台にあるのが、みなさんのコンテンツへの安全なアクセスです。その上に、使えるAIモデル、与えられた指示、達成したい目的、そして利用できる機能やツールが重なります。コンテンツへのアクセスはBoxの権限の範囲内で行われ、指示の書き方によって結果が大きく変わります。Box上に資料が整理されているほど、検索や要約、一覧化、下書き作成などに活用しやすくなります。

### Scene 04:点在する資料の横断整理

Box内に点在する複数の資料を、横断して参照することもできます。物件資料や価格表、PJコード一覧、サービス枠管理表に加えて、広告宣伝費、工程表、収支表、契約書など、フォルダをまたいで散らばっている資料をAIがまわりながら参照します。そのうえで、物件ごとの基本情報や価格、PJコード、確認事項をひとつにまとめて出力します。資料を一つずつ開いて確認する手間を減らし、情報整理の初動を早めることができます。

### Scene 05:PJコードが共通キー

Box内の資料をAIで活用しやすくするためには、共通のキーとなる情報を意識して保存することが重要です。当社では物件ごとにPJコードが紐づいているため、資料名やフォルダ名、ファイル内の記載には、できるだけPJコードを含めるようにしてください。これにより、異なる資料をまたいだ参照や紐づけがしやすくなり、後から検索・抽出・集約する際にも、必要な情報を正確に引き出しやすくなります。

### Scene 06:具体的な使い方

具体的な使い方としては、対象となるファイルやフォルダを選び、その内容について質問します。このフォルダ内の資料を要約してください、物件名とPJコードを一覧化してください、価格情報を整理してください、サービス枠管理表からこの物件の残額を確認してください、稟議・決裁資料の下書きを作成してください、といった形です。ただし、今できるのはここまでで、出力形式もまだ限定的です。実際のファイルを編集するなど、タスクそのものを完了させることはまだできません。

### Scene 07:実際にやってみた例

実際にやってみた例です。物件リストにPJコードと価格を付与し、顧客情報を紐づけてみました。物件概要が収納されているフォルダを指定して、中のファイルの概要を見やすくHTMLにまとめてみました。さらに、フォルダ内の事業収支や収支経緯表、サービス枠管理表を分析させて、決裁の定型文に合わせた文章と数字を出力してみました。このまま、ラクモのワークフローに貼る形に調整できます。点在する情報を、確認しやすい形に整理する用途で活用できます。

### Scene 08:Boxへ集める

AIは、それぞれのBox権限の中で動きます。そのため、自分の権限が及ぶ範囲に情報を蓄積していくイメージで資料を集めてください。その際は、AIが突合させやすいように、PJコードなど一定のキーを中心に整理しながら格納すると、後から探しやすくなります。なお、GoogleワークスペースとBoxの連携は許可されているため、Gmailの本文をBoxに蓄積する機能もあります。

### Scene 09:プロンプトのコツ

プロンプトの書き方も重要です。対象となる資料、確認したい項目、出力の形を、そのまま具体的に書いてください。例えば、このフォルダの内容を突合して、決裁資料用の売価、税額、サービス枠残額、粗利などの数字をまとめて表示してください、といった形です。見てほしい資料と、欲しい項目、出したい形を丁寧に書くほど、実務で使いやすい結果になります。

### Scene 10:毎回の説明が必要

注意点です。このAIは、背景などのコンテキストをまだ蓄積できません。そのため、毎回のプロンプトでしっかり説明する必要があります。特に初回のプロンプトが重要になりますので、事前にチャットGPTなどを活用してプロンプトを作成してから使ってみてください。うまく出力できたプロンプトは、ぜひ共有していきましょう。

### Scene 11:1日の利用制限

また、BOX AIには1日の利用制限があります。制限を超えると、動作するモデルが下がります。この赤い表示が出たら、Box Agent Liteで動作している状態です。モデルが下がった後は、横断整理や複雑な下書きなどの重い作業は避けて、作業は軽めにしておきましょう。今後、プロンプトを含めて改善されていくと思います。

### Scene 12:まずは身近なところから

まずは身近なフォルダや管理表から、無理のない範囲で試してみてください。Box上に資料を整理・蓄積していくことが、今後の活用につながります。活用事例やうまくいったプロンプトは、ぜひご共有ください。

## 16. 変更履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1.0 | 2026-08-14 | 実装済みHTML動画を基に初版作成。10シーン、Edge音声合成、実例保存、Lite警告を反映 |
| 1.1 | 2026-08-15 | 実装の刷新に追随。シーン構成を全面改訂(仕組み・使い方・Boxへ集約を追加)、音声を事前生成MP3(edge-tts / Keita)+同期字幕方式へ変更、素材フォルダを`assets/`へ変更、`index.html`・`.nojekyll`によるWeb公開構成とZIP配布を整理 |
| 1.2 | 2026-08-15 | キネティック・タイポグラフィ演出へ全面リデザイン(`data-at`/`data-out`によるJS駆動同期)。Scene1のBox実画面を右半分中央に大型配置、Scene4・5を中央揃え化、字幕を下部専用帯へ移動して本文との重なりを解消、再生速度1/1.25/1.5倍切替を追加、操作パネルを自動非表示の右下配置へ変更、実例HTML保存ボタンを大型化、Box公式サイト・ユースケース資料へのリンクを追加、Scene8にプロンプト再利用(ChatGPT/GPTs/Box AI登録)の補足を追加 |
| 1.3 | 2026-08-15 | タイトルを「検索だけじゃない、BOX AI Agent」に変更。パンくず(チャプター)を追加、操作パネルをタップでトグル+自動非表示に変更。Scene3を資料オービット演出へ刷新し広告宣伝費・工程表・収支表・契約書を追加、Scene5に稟議・決裁資料の下書きと現状の制約を追記、Scene6を実例3件に更新、Scene7に権限の範囲・キー中心の格納・GWS連携を追加、Scene8を実プロンプト例に差し替え、利用時の注意をScene9(コンテキスト)とScene10(1日の利用制限)に分割、Scene11に活用事例の共有依頼を追加。原稿を`tools/script_segments.py`へ分離し、`data-plan`による尺スケーリングと音声なし再生モードを追加 |
| 1.4 | 2026-08-15 | Box公式ビジュアル2点を採用。Scene02を`box-ai-hub.png`(あらゆる形式の資料×複数のAIモデル)基調に再構成し、Scene03「エージェントの構成」(`box-ai-agent-layers.png`)を新設して全12シーンに。MP3が無い場合にWeb Speech APIの日本語音声で読み上げる再生モードを追加(読み上げ位置に字幕・演出を同期) |
| 1.4.1 | 2026-08-15 | Scene07の活用例③を「事業収支・収支経緯表・サービス枠管理表を分析し、決裁の定型文に合わせて出力(そのままラクモのワークフローに貼れる形)」に更新 |
| 1.5 | 2026-08-15 | 開始ゲートを廃止して自動再生に変更（音声はスピーカーボタンでON）。音声ボタンを音符からスピーカーアイコンへ変更し、OFFの間は点滅で位置を示すようにした。ブラウザ読み上げはEdgeの「Microsoft Keita Online (Natural)」を最優先で選択。Windows向けに`tools/gen_audio.bat`を追加 |
